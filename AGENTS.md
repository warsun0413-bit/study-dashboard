# AGENTS.md

## 项目名称

南开马理论考研任务面板

## 项目目标

本项目用于帮助用户完成每日英语、马原、马发史、南开论述题、课设/答辩推进，并通过晚上复盘发现拖延点。

所有后续开发都应围绕“提升考研执行效率”展开，避免为了复杂而继续堆功能。

## 当前技术栈

- HTML
- CSS
- JavaScript
- localStorage
- Git
- GitHub

## 项目边界

当前项目不是后端项目。

不要引入或假设以下技术：

- Django
- MySQL
- Node.js 后端
- 任何需要服务器部署的后端框架

除非用户明确要求，否则不要把本项目改造成前后端分离项目、数据库项目或账号系统。

## 开发前规则

1. 修改前必须先运行 `git status`。
2. 较大功能必须先提醒用户创建或切换到 `feature` 分支。
3. 修改前必须先给出实现计划，并等待用户确认。
4. 未经用户确认，不要直接修改文件。
5. 每次修改范围应尽量小，优先保持当前稳定功能不受影响。

## 数据安全规则

1. 不允许清空历史复盘数据。
2. 不允许清空用户已有的 `localStorage` 数据。
3. 不允许修改、生成或上传 JSON/TXT 备份文件。
4. 不允许修改项目目录外的文件。
5. 不允许 Codex 创建、读取、展示或提交真实 API Key、访问令牌、密钥文件或凭据内容。
6. 不允许把密钥写入前端文件，例如 `index.html`、`script.js`、`manifest.json` 或任何浏览器可直接查看的文件。
7. 如需演示密钥配置，只能使用占位符，例如 `YOUR_API_KEY_HERE`、`sk-xxxx` 或 `example_key`。
8. 如果发现疑似密钥，只能提醒用户存在泄露风险，不要在回复中复述完整密钥。
9. 不允许破坏现有功能，包括：
   - 今日任务勾选
   - 今日完成率
   - 番茄钟
   - 晚上复盘
   - 历史复盘记录
   - 本周学习统计
   - TXT 导出
   - JSON 备份与恢复

## 修改后规则

1. 修改后必须说明改了哪些文件。
2. 修改后必须说明主要改动内容。
3. 如能检查，应运行必要的检查命令，例如 `git diff --check` 和 `git status --short`。
4. 测试通过后，提醒用户执行 `git commit`。
5. 如果用户需要同步到 GitHub，提醒用户执行 `git push`。

## 风格原则

1. 优先帮助用户降低每天开始任务、判断完成、复盘拖延原因的成本。
2. 小改动可以直接在 `main` 上进行，但仍必须先运行 `git status` 并给出计划。
3. 大功能应使用 `feature` 分支开发。
4. 不主动引入复杂依赖。
5. 不为了技术炫技牺牲项目的简单、稳定和可维护性。
