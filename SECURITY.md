# 安全说明

本项目目前是纯前端学习面板，主要数据保存在浏览器 `localStorage` 中。后续如果接触第三方模型 API、Codex 相关 API 或其他需要密钥的服务，必须先保护 API Key 和本地密钥文件。

## API Key 使用规则

1. API Key 不能写进前端代码，例如 `index.html`、`script.js` 或浏览器可直接查看的文件。
2. API Key 不能提交到 GitHub。
3. API Key 不能放进 `README.md`、`START.md`、教程截图或任何公开文档。
4. API Key 不能截图公开，也不要在聊天、邮件、Issue、Pull Request 中粘贴真实密钥。
5. 如果需要写示例，只能使用占位符，例如 `YOUR_API_KEY_HERE`、`sk-xxxx` 或 `example_key`。

## 推荐处理方式

如果后续确实需要接入第三方 API，不要让浏览器前端直接持有真实密钥。

更安全的方式是：

- 通过后端服务转发 API 请求。
- 或使用本地安全配置文件，并确保配置文件被 `.gitignore` 忽略。
- 或使用系统环境变量读取密钥。

本项目当前不是后端项目，不应为了 API Key 直接把密钥写入前端文件。

## 已忽略的敏感文件

`.gitignore` 已配置忽略以下内容：

- `.env`
- `.env.*`
- `*.key`
- `secrets/`
- `credentials/`

这些文件和目录用于存放本地环境变量、密钥或凭据时，不应提交到 GitHub。

## 如果误提交了密钥

如果不小心把真实 API Key 或密钥文件提交到 GitHub，应立即：

1. 到对应服务平台撤销或轮换该密钥。
2. 删除仓库中的密钥文件。
3. 检查 Git 历史中是否仍可看到密钥。
4. 必要时清理 Git 历史，并重新生成新的密钥。

不要只删除当前文件就认为已经安全，因为密钥可能已经出现在提交历史中。
