# PROMPTS.md

## 使用说明

这个文件保存以后和 Codex 协作时常用的提示词模板。

使用方法：

1. 复制需要的模板。
2. 把方括号里的内容替换成你的实际需求。
3. 如果涉及修改文件，记得要求 Codex 先运行 `git status`，并先给计划，等你确认后再执行。

## 1. 新增功能模板

```text
请给当前 study-dashboard 项目新增一个功能。

背景：
[说明当前项目状态，例如：这是一个南开马理论考研任务面板，已有今日任务、番茄钟、复盘、历史记录、JSON 备份等功能。]

目标/问题：
[说明要新增什么功能，以及为什么需要它。]

限制：
1. 修改前先运行 git status。
2. 修改前先给我实现计划，等我确认后再执行。
3. 只允许修改：[列出允许修改的文件]。
4. 不要影响现有功能：[列出不能破坏的功能]。
5. 不要清空 localStorage 或历史复盘数据。
6. 如果是较大功能，请提醒我先创建 feature 分支。

验收标准：
1. 页面能正常打开。
2. 新功能能按预期使用。
3. 原有功能不受影响。
4. 修改后说明改了哪些文件。
5. 测试通过后提醒我 git commit 和 git push。
```

## 2. 修复 BUG 模板

```text
请帮我修复当前 study-dashboard 项目的一个 BUG。

背景：
[说明 bug 出现在哪个功能里，例如：番茄钟、任务勾选、复盘保存、JSON 导入导出。]

目标/问题：
[具体描述 bug，例如：点击某个按钮后没有反应，刷新后数据没有恢复，导出文件内容不正确。]

限制：
1. 修改前先运行 git status。
2. 修改前先给我修复计划，等我确认后再执行。
3. 只允许修改：[列出允许修改的文件]。
4. 不要改动无关功能。
5. 不要清空历史复盘记录。
6. 不要修改项目外文件。

验收标准：
1. bug 被修复。
2. 原有功能仍然正常。
3. 修改范围尽量小。
4. 修改后说明改了哪些文件。
5. 测试通过后提醒我 git commit 和 git push。
```

## 3. 只分析不修改模板

```text
请阅读当前 study-dashboard 项目，只分析，不修改任何文件。

背景：
[说明你希望 Codex 从什么角度看项目，例如：产品经理视角、代码结构视角、学习效率视角、数据安全视角。]

目标/问题：
[列出你希望回答的问题。]

限制：
1. 不要修改任何文件。
2. 不要运行破坏性命令。
3. 不要清空或改动 localStorage。
4. 如果需要引用代码，请说明来自哪个文件。

验收标准：
1. 回答要基于当前项目实际内容。
2. 结论要清晰，适合新手理解。
3. 如果提出优化建议，请按优先级排序。
4. 不要直接开始修改。
```

## 4. 样式优化模板

```text
请优化当前 study-dashboard 项目的页面样式。

背景：
[说明当前页面哪里看起来不够好，例如：移动端拥挤、按钮不清晰、卡片层级不明显。]

目标/问题：
[说明希望达到的视觉效果，例如：更紧凑、更清晰、手机端不乱、重点信息更突出。]

限制：
1. 修改前先运行 git status。
2. 修改前先给我样式优化计划，等我确认后再执行。
3. 优先只修改 style.css。
4. 不要改变功能逻辑。
5. 不要修改 localStorage 数据结构。
6. 不要清空历史复盘记录。

验收标准：
1. 页面整体视觉更清晰。
2. 手机和平板排版不乱。
3. 原有功能不受影响。
4. 修改后说明改了哪些文件。
5. 测试通过后提醒我 git commit 和 git push。
```

## 5. 数据安全修改模板

```text
请对当前 study-dashboard 项目做一次数据安全相关修改。

背景：
[说明数据相关功能，例如：localStorage、历史复盘、JSON 备份/恢复、TXT 导出。]

目标/问题：
[说明要解决什么数据安全问题，例如：避免误覆盖、导入前确认、备份内容更完整。]

限制：
1. 修改前先运行 git status。
2. 修改前先给我实现计划，等我确认后再执行。
3. 不允许清空历史复盘数据。
4. 不允许清空已有 localStorage 数据。
5. 不允许改变现有历史复盘数据结构，除非先说明原因并等我确认。
6. 不允许修改或上传 JSON/TXT 备份文件。
7. 不允许修改项目外文件。

验收标准：
1. 数据不会被静默删除或覆盖。
2. 重要操作前有确认或明确提示。
3. JSON 备份/恢复仍然可用。
4. 原有复盘记录仍然可读取。
5. 修改后提醒我 git commit 和 git push。
```

## 6. Git 提交前检查模板

```text
请帮我做 Git 提交前检查，只检查，不修改文件。

背景：
[说明这次改动大概是什么，例如：新增 README、优化样式、增加番茄钟、修复导入 JSON。]

目标/问题：
确认当前工作区改动是否符合预期，是否有不该提交的文件。

限制：
1. 先运行 git status。
2. 可以运行 git diff 查看改动。
3. 不要修改任何文件。
4. 不要执行 git add、git commit、git push，除非我明确要求。
5. 不要还原文件。

验收标准：
1. 告诉我当前分支。
2. 告诉我哪些文件被修改、新增或删除。
3. 总结主要改动。
4. 指出是否有不该提交的文件，例如 JSON/TXT 备份文件、日志文件。
5. 如果可以提交，给出建议的 git add 和 git commit 命令。
```

## 7. 产品审查模板

```text
请把当前 study-dashboard 项目当作真实产品来审查，只分析，不修改文件。

背景：
产品名称：南开马理论考研任务面板
产品目标：帮助我完成每日英语、马原、马发史和南开论述题推进，并通过复盘发现拖延点。

目标/问题：
[列出要审查的问题，例如：主链路是否清晰、核心功能是否够用、下一版最值得优化什么。]

限制：
1. 不要修改任何文件。
2. 不要运行破坏性命令。
3. 不要建议复杂后端、账号系统、数据库，除非我明确要求。
4. 优先从考研执行效率角度分析。

验收标准：
1. 明确指出当前 MVP 是否清晰。
2. 区分核心功能和不该继续堆的功能。
3. 判断当前问题是功能不足，还是执行标准不清楚。
4. 给出下一版最值得优化的 3 个点。
5. 建议要具体、可执行、不要过度复杂。
```
